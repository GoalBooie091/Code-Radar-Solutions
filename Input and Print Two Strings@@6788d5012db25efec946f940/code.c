#include <stdio.h>

char* welcome() {
    return "Welcome to Code Radar!";
}

int main() {
    char text[] = 'Hello';
    scanf("%s",&text);
    char goal[] = 'World';
    scanf("%s",&World);
    printf("You entered: %s and %s",text,goal);
    return 0;
}